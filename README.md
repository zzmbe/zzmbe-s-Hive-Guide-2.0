# Bee Swarm Guide

A free static website for a Bee Swarm Simulator personalized progression guide.

## Run locally
Open `index.html` in a browser.

## Publish for free
Use GitHub Pages. Put `index.html`, `style.css`, and `script.js` in a repository and enable Pages from the repository settings.
